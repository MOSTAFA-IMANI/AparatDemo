<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.cardview.widget.CardView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/item_video_main_home_card_margin"
        app:cardCornerRadius="@dimen/item_video_main_home_card_radius">
        <RelativeLayout
            android:layout_width="@dimen/item_video_main_home_card_width"
            android:layout_height="@dimen/item_video_main_home_card_height">


            <ImageView
                android:id="@+id/image_view"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                />

            <View
                android:layout_width="match_parent"
                android:layout_height="40dp"
                android:layout_alignBottom="@id/image_view"
                android:background="@drawable/gradient" />

            <TextView
                android:id="@+id/text_view_user_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_alignBottom="@id/image_view"
                android:layout_marginStart="6dp"
                android:layout_marginTop="6dp"
                android:layout_marginEnd="6dp"
                android:layout_marginBottom="6dp"
                android:drawablePadding="2dp"
                android:ellipsize="end"
                android:gravity="right"
                android:maxLines="1"
                android:textColor="#fff"
                android:textSize="12dp"
                tools:text="username" />

        </RelativeLayout>
    </androidx.cardview.widget.CardView>

</layout>
